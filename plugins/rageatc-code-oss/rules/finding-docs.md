# Finding Documentation

When the user asks about a library, framework, SDK, API, CLI tool, or cloud service — including API syntax, configuration, version migration, setup, or debugging — load the `finding-docs` skill. Prefer this over web search and training data for library documentation.
