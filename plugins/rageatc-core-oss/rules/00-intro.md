# rageatc-core-oss Operating Rules

The sections below are the operating rules for the rageatc-core-oss plugin. Read them carefully and follow them throughout this session.

If this output appears truncated (a short preview plus a file path instead of the full rules), read the referenced file to recover the complete content before proceeding.
